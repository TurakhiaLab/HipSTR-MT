name: Build and test

on:
  push:
    branches: [main, development]
  pull_request:
  workflow_dispatch:

jobs:
  build-and-test:
    # Pinned rather than ubuntu-latest, which moves to Ubuntu 26 on 2026-10-19:
    # package names in the apt step can change between releases, and this is
    # the environment the README's install command is verified against.
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      # Matches README.md's "Requirements" section exactly, so a CI failure
      # here means the documented install instructions are themselves wrong.
      # Note: no libhts-dev -- htslib is vendored and built from source from
      # lib/htslib, so a system htslib package is never used (see README).
      - name: Install system dependencies
        run: |
          sudo apt-get update -o Acquire::Retries=3
          sudo apt-get install -y make g++ cmake zlib1g-dev libbz2-dev liblzma-dev libcurl4-openssl-dev libssl-dev

      - name: Build (plain make, not PGO -- see Makefile's PGO section)
        run: make -j"$(nproc)"

      - name: Run test suite
        run: test/run_tests.sh

      - name: Upload plots/logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-output
          path: |
            test/pgo/actual_output_t*.vcf.gz
            test/pgo/actual_output_t*.log
          if-no-files-found: ignore
